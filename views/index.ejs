<% include ./partials/header %>

  <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <div class="carouselHeader"><h4>Ending Soon...</h4></div>
    <!-- Indicators -->
    <ol class="carousel-indicators">
    <%  if(obj.length > 0) { %>
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
    <% } %>

    <%  for(var i = 1; i < 5 && i < obj.length; i++) { %>
        <li data-target="#myCarousel" data-slide-to="<%=i%>"> </li>
    <% } %>
    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner">
    <%  if(obj.length > 0) { %>
      <div class="item active">
        <img class="soulImage" src=<%= obj[i].default_img %> alt=<%= obj[i].soul_name %>>
        <div class="carousel-caption">
          <a class="carouselLink" href="/listings/<%= obj[i].listing_id %>"><h3><%= obj[i].soul_name %></h3></a>
          <h4><%= obj[i].soul_score %></h4>
          <p><%= obj[i].description%></p>
        </div>
      </div>
    <% } %>

    <%  for(var i = 1; i < 5 && i < obj.length; i++) { %>
        <div class="item">
            <img class="soulImage" src=<%= obj[i].default_img %> alt=<%= obj[i].soul_name %>>
        <div class="carousel-caption">
          <a class="carouselLink" href="/listings/<%= obj[i].listing_id %>"><h3><%= obj[i].soul_name %></h3></a>
          <h4><%= obj[i].soul_score %></h4>
          <p><%= obj[i].description%></p>
        </div>
      </div>
    <% } %>
  
    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
      <span class="glyphicon glyphicon-chevron-left"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
      <span class="glyphicon glyphicon-chevron-right"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>

<!-- Main listings -->
<h3>Available Souls!</h3>
<% for(var i = 5; i < obj.length; i++){ %>
    <div class='soulListing'>
        <div class = "listHeading">
            <div class="imgThumb">
                <img src=<%= obj[i].default_img %> class="listImage" />
            </div>
            <div class="soulName">
                <a class="item" href="/listings/<%= obj[i].listing_id%>">
                    <h3 class="listHead"><%= obj[i].soul_name %>: Soul Class: 
                         <%= obj[i].soul_score %>
                    </h3>
                </a>
                <p class="desc"><%= obj[i].description %></p>
                Current Bid: </strong><%= '$'+ (obj[i].currBidCents/100).toFixed(2) %>
            </div>
            <div class="dt"> Ends: <%= obj[i].endDatetime %></div>
        </div>
        <hr>
    </div> 
<% } %>
<% include ./partials/footer %>